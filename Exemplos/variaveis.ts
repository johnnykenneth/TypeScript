
// VARIÁVEIS DO TIPO STRING

// Armazena um nome
let personagem: string = "Link";

// Armazena o nome de um jogo
let jogo: string = "The Legend of Zelda";

// Armazena uma frase simples
let mensagem: string = `Bem-vindo ao jogo!`;

// VARIÁVEIS DO TIPO NUMBER

// Número inteiro representando o nível do personagem
let nivelPersonagem: number = 15;

// Quantidade de moedas coletadas
let moedasColetadas: number = 120;

// Número decimal representando a vida restante
let vidaRestante: number = 73.5;

// VARIÁVEIS DO TIPO BOOLEAN

// Indica se o personagem tem uma espada (true = sim)
let temEspada: boolean = true;

// Indica se o personagem está vivo
let estaVivo: boolean = true;

// Define se o modo difícil do jogo está ativado (false = não)
let modoHardAtivado: boolean = false;
